# callbacks are a self-contained set of functions to extend the existing functionality 
# examples include update learning rate, early stopping, logging, etc. sending email once training is done
# callbacks hook are called at different stages of training e.g at the start of training, at the end of each epoch, at the end of training